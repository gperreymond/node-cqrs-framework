machine:
  timezone:
    Europe/Paris
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

dependencies:
  pre:
    - pip install docker-compose

test:
  pre:
    - npm run docker:test

notify:
  webhooks:
    - url: https://hooks.slack.com/services/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
